# Staging-environment overrides.
# Apply with: helm upgrade --install gitflow-analyzer . -f values.yaml -f values-staging.yaml

analyzer:
  env:
    ENVIRONMENT: staging

graphBuilder:
  env:
    ENVIRONMENT: staging

# serviceAccount.annotations will be populated after the IRSA role is created:
#   serviceAccount:
#     annotations:
#       eks.amazonaws.com/role-arn: arn:aws:iam::153772056450:role/gitflow-analyzer-staging-app-role
