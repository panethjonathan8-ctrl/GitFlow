flask==3.0.3
# Flask is the web framework — handles HTTP requests and routing.
# We pin to an exact version so the app behaves identically
# on your laptop, in CI, and on the EC2 instance.

boto3==1.34.144
# boto3 is the official AWS SDK for Python.
# Used to fetch the GitHub token from Secrets Manager at runtime.

gitpython==3.1.43
# GitPython lets you clone and inspect git repositories in Python.
# The analyzer uses this to clone the target repo into a temp folder.

requests==2.32.3
# HTTP library — used to call the GitHub API to get repo metadata.

gunicorn==22.0.0
# Gunicorn is a production-grade WSGI server for Python.
# Flask has a built-in dev server but it handles one request at a time.
# Gunicorn handles multiple concurrent requests properly.
# You never run Flask's built-in server in production.
